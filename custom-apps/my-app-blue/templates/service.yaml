apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-app-blue.fullname" . }}
  labels:
    {{- include "my-app-blue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-app-blue.selectorLabels" . | nindent 4 }}
